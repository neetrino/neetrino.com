/** Hero stat numerals — slow count-up when stats enter the viewport. */

export const COUNT_UP_DURATION_MS = 3400;

export const COUNT_UP_STAGGER_MS = 480;

/** Ease-out exponent for count progress (higher = softer landing). */
export const COUNT_UP_EASE_OUT_POWER = 3;
