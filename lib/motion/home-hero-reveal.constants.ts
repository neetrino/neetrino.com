/** Desktop + mobile home hero — slower, smoother entrance reveals. */

export const HOME_HERO_REVEAL_DURATION_MS = 1040;

/** Vertical travel on fade-in (px) — smaller rise reads softer at long duration. */
export const HOME_HERO_REVEAL_Y_PX = 28;

/** Stagger between hero layers (logo → robot → copy → stats). */
export const HOME_HERO_REVEAL_DELAY_LOGO_MS = 0;

export const HOME_HERO_REVEAL_DELAY_ROBOT_MS = 420;

export const HOME_HERO_REVEAL_DELAY_BODY_MS = 780;

export const HOME_HERO_REVEAL_DELAY_CTA_MS = 940;

export const HOME_HERO_REVEAL_CTA_STAGGER_MS = 80;

export const HOME_HERO_REVEAL_DELAY_STATS_MS = 1160;

export const HOME_HERO_REVEAL_DELAY_STAT_WIDE_MS = 1320;

/** Mobile title lines (`NEET` / `RIN` / `O`) — gap after previous line (ms). */
export const HOME_HERO_TITLE_LINE_STAGGER_MS = 160;

/** Mobile body copy entrance (after title block). */
export const HOME_HERO_MOBILE_BODY_DELAY_MS = 520;
