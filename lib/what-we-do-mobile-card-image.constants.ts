/** Mobile What we do — art read for cards 1–3 (slightly larger than previous scales). */
export const WHAT_WE_DO_MOBILE_CARD_WEBSITE_IMAGE_SCALE_CLASS = "scale-[1.60]" as const;
export const WHAT_WE_DO_MOBILE_CARD_MOBILE_APP_IMAGE_SCALE_CLASS = "scale-[1.52]" as const;
export const WHAT_WE_DO_MOBILE_CARD_SAAS_IMAGE_SCALE_CLASS = "scale-[1.25]" as const;

/** PC + cloud tiles: nudge art slightly right on mobile (object-left baseline). */
export const WHAT_WE_DO_MOBILE_CARD_PC_CLOUD_IMAGE_TRANSLATE_X_CLASS = "translate-x-1.5" as const;
