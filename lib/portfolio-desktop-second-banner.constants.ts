/**
 * Second desktop hero banner — same frame as the first card for alignment.
 * Art (`imgBiotechLogo1`) uses `object-cover` in the scene.
 */
export {
  PORTFOLIO_FIRST_BANNER_FRAME_CLASS as PORTFOLIO_SECOND_BANNER_FRAME_CLASS,
  PORTFOLIO_FIRST_BANNER_WIDTH_PX as PORTFOLIO_SECOND_BANNER_WIDTH_PX,
} from "./portfolio-desktop-first-banner.constants";
