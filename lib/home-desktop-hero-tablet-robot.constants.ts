/**
 * Tablet hybrid home hero (`showTabletHero450Hand`): robot asset `10:425` — lower than Figma `top-[45px]`
 * so the figure clears the fixed `MobileHeader` on iPad mini width.
 * **Literal** for Tailwind JIT.
 */
export const HOME_DESKTOP_HERO_TABLET_ROBOT_WRAPPER_TOP_CLASS = "top-[96px]" as const;
