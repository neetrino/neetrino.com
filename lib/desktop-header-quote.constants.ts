/**

 * Figma desktop header bar — “Get a Quote” pill (nodes 10:451–10:452).

 * `DESKTOP_HEADER_QUOTE_LEFT_PX` is derived with the nav pill so the CTA clears Contact.

 */

export { DESKTOP_HEADER_QUOTE_LEFT_PX } from "@/lib/desktop-header-nav-pill.constants";

export const DESKTOP_HEADER_QUOTE_TOP_PX = 8;
