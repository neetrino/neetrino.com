/** `next/image` `sizes` for project cards in the home flow grid — same as mobile portfolio. */
export { MOBILE_PORTFOLIO_CARD_IMAGE_SIZES as HOME_PROJECTS_GRID_IMAGE_SIZES } from "@/lib/portfolio/portfolio-image-sizes";

/** `next/image` `sizes` for project cards in the desktop home marquee strip (631px card width). */
export { PUBLIC_PORTFOLIO_DESKTOP_IMAGE_SIZES as HOME_PROJECTS_MARQUEE_IMAGE_SIZES } from "@/lib/constants/public-portfolio-pagination.constants";
