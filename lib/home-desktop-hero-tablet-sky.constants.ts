/**
 * Tablet hybrid home hero (`showTabletHero450Hand`): extends Figma `10:422` sky / atmosphere block upward
 * so more blue sits under the fixed translucent `MobileHeader` (vs flat `#151515`).
 * **Literals** for Tailwind JIT (`916` = `844` + `72` design bleed).
 */
export const HOME_DESKTOP_HERO_TABLET_SKY_FRAME_OUTER_CLASS = "-top-[72px] h-[916px]" as const;

export const HOME_DESKTOP_HERO_TABLET_SKY_FRAME_INNER_CLASS = "h-[916px]" as const;
