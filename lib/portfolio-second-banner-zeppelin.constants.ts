/**
 * Figma node `525:1727` — ZEPPELIN lockup on the second desktop portfolio hero card.
 * Fill `#f6ff00`, Orbitron Black 900; Figma type size 55px — scaled for bottom-right badge.
 */
export const PORTFOLIO_SECOND_BANNER_ZEPPELIN_COPY = "ZEPPELIN" as const;

export const PORTFOLIO_SECOND_BANNER_ZEPPELIN_TEXT_SIZE_CLASS = "text-[49px]" as const;

/** Inset from the card’s right edge — shifts the lockup left. */
export const PORTFOLIO_SECOND_BANNER_ZEPPELIN_CORNER_WRAPPER_CLASS =
  "pointer-events-none absolute bottom-1 right-[25px] z-10 px-3.5 pt-2 pb-2.5" as const;
