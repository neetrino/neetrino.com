/**
 * Bounding box of the center 3D asset in `AboutUsFigmaBlock2` (Figma 335:1190).
 * The design-options palette uses the same frame so it matches adjacent hero icons.
 */
export const ABOUT_DESIGN_OPTIONS_PALETTE_FRAME_CLASS = "h-[191px] w-[209px]";

/**
 * Lifts the in-row palette toward the floating 3D icons.
 */
export const ABOUT_DESIGN_OPTIONS_PALETTE_TRANSLATE_Y_CLASS = "-translate-y-[130px]";

/** HY/RU “100k design options” lines — 2px lower than the palette lift so copy sits slightly under the art. */
export const ABOUT_DESIGN_OPTIONS_FEATURE_COPY_TRANSLATE_Y_CLASS = "-translate-y-[100px]";

/**
 * Figma 335:1178 — shifts the whole four-column caption row up toward the floating 3D icons
 * (all feature lines together, not only the palette column).
 */
export const ABOUT_WHY_CHOOSE_FEATURE_LABELS_ROW_TRANSLATE_Y_CLASS = "-translate-y-[68px]";
