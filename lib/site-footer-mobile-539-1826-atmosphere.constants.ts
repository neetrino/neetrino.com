/**
 * Mobile footer backdrop — Figma NEETRINO-WEB `539:1826` (Ellipse 31).
 * Implementation: `app/globals.css` → `.site-footer-mobile-backdrop-atmosphere` (Tailwind does not
 * scan arbitrary utilities in this `lib/` file, so the gradient must live in global CSS).
 */
export const SITE_FOOTER_MOBILE_539_1826_ATMOSPHERE_CLASS =
  "site-footer-mobile-backdrop-atmosphere" as const;
