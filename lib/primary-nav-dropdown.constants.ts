/** Delay before opening on hover — reduces accidental flicker when crossing the nav. */
export const PRIMARY_NAV_DROPDOWN_OPEN_DELAY_MS = 120;

/** Delay before closing after pointer leaves (allows re-entry without flash). */
export const PRIMARY_NAV_DROPDOWN_CLOSE_DELAY_MS = 150;

/** Panel enter/exit motion — matches spec (150–200ms, ease-out). */
export const PRIMARY_NAV_DROPDOWN_TRANSITION_MS = 180;
