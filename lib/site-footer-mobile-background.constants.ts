/**
 * Mobile footer vector `479:1145` (NEETRINO-WEB) — grid + radial stroke in SVG; `mix-blend-overlay` on host.
 * Inner frame matches Figma dev handoff `inset-[-0.11%_0]` so the pattern tiles edge-to-edge when scaled.
 */
export const SITE_FOOTER_MOBILE_VECTOR_BLEND_HOST_CLASS = "mix-blend-overlay" as const;

export const SITE_FOOTER_MOBILE_VECTOR_FRAME_CLASS =
  "absolute inset-x-0 top-[-0.11%] bottom-[-0.11%] w-full" as const;

export const SITE_FOOTER_MOBILE_VECTOR_IMAGE_CLASS =
  "max-w-none object-cover object-center" as const;
