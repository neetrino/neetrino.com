/** Idle timeout for mounting full desktop canvas scene after route open (ms). */
export const DESKTOP_SCENE_MOUNT_IDLE_TIMEOUT_MS = 180;

/** Idle timeout for non-critical decorative layers inside a scene (ms). */
export const DESKTOP_DECOR_DEFER_IDLE_TIMEOUT_MS = 100;

/** `requestIdleCallback` timeout for services light-rays decorative images (ms). */
export const SERVICES_LIGHT_RAYS_DECOR_IDLE_TIMEOUT_MS = 1200;
