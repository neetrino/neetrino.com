/**
 * /services mobile: same card chrome as desktop (rounded-[38px], gradients, orbit art),
 * full-width flow — no fixed 369×463 canvas box.
 */
export const SERVICES_MOBILE_CATALOG_CARD_OUTER_CLASS =
  "relative mx-auto flex w-full max-w-[540px] min-w-0 flex-col items-start gap-[30px] overflow-clip rounded-[38px] px-6 py-10 sm:px-8 sm:py-12 md:px-10 md:py-[60px] min-h-[26rem] sm:min-h-[28rem] md:min-h-[463px]" as const;
