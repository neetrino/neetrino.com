/** Locale-dependent width/arrow position for desktop footer send CTA (Figma `10:342`). */
export type SiteFooterDesktopSendCtaLayout = {
  readonly footerSendWidthClassName: string;
  readonly footerSendIconLeftClassName: string;
};
